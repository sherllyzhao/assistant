# Sherlly 数字秘书路线图

## V1（必须实现）

目标：

**保证微信里的工作事项不会被遗忘。**

### 任务录入

#### 手动录入

* 快捷键速记
* 粘贴微信内容
* 拖拽文本创建任务
* 上传图片和其他附件

#### 自动发现

* 微信消息扫描
* AI识别待办
* 候选任务池
* 确认后入库

---

### 任务管理

状态：

```text
待办
进行中
等待他人
已完成
已取消
```

属性：

```text
标题
来源
负责人
创建时间
截止时间
优先级
标签
备注
每日时段
执行动作
附件
```

---

### 任务执行

目标：

```text
点击开始执行
↓
自动打开任务对应的工作环境
↓
记录开始执行日志
```

执行动作：

* 打开网址
* 打开文件或文件夹
* 打开指定软件
* 用 VSCode 打开指定项目

示例：

```text
报价任务
↓
打开客户报价网站
```

```text
开发任务
↓
用 VSCode 打开项目目录
```

边界：

```text
V1 只负责打开目标环境
V2/V3 再考虑自动点击、自动填写、自动提交
```

---

### 提醒系统

提醒级别：

```text
高优先级
15分钟

普通
30分钟

低优先级
60分钟
```

提醒方式：

* Windows通知
* 托盘闪烁
* 声音提醒（可关闭）

---

### 遗忘检测

发现：

```text
微信出现任务
↓
未录入
↓
30分钟后提醒
```

提示：

```text
发现可能遗漏的事项
```

---

### 工作日志

自动记录：

```text
创建任务
修改任务
完成任务
删除任务
```

生成：

```text
今日日志
本周日志
本月日志
```

---

### 线上存储

目标：

```text
本地任务数据
↓
后台 API
↓
MongoDB Atlas
```

同步范围：

* 任务
* 候选任务
* 工作日志
* 用户设置

安全边界：

```text
MongoDB 连接串只放后台 .env
桌面端不直接连接 MongoDB
```

---

## V2（效率提升版）

目标：

**减少人工确认。**

### 智能优先级

自动判断：

```text
今天
高

本周
中

月底
低
```

---

### 自动截止时间识别

例如：

```text
周五前
```

自动识别：

```text
周五18:00
```

---

### 联系人识别

例如：

```text
王总：
周三发报价
```

自动关联：

```text
客户
王总
```

---

### Android同步

同步：

* 任务
* 提醒
* 日志

手机可查看：

```text
今日待办
```

---

### 语音速记

手机：

```text
提醒我下午问王总合同
```

自动建任务。

---

### AI任务整理

每天自动：

```text
整理重复任务

合并相似任务

识别长期未完成任务
```

---

### 安全速记 / 密码保险箱

目标：

```text
临时或长期记录账号、密码、密钥、门禁信息等敏感资料
↓
需要时快速查找
↓
默认隐藏，确认后短时显示
```

典型内容：

* 网站账号密码
* 客户系统登录信息
* 软件 License / 激活码
* API Key / Token
* Wi-Fi / VPN / FTP / SFTP / 服务器登录信息
* 其他不适合放进普通任务备注的敏感速记

字段：

```text
标题
分类
账号
密码
网址
备注
标签
创建时间
更新时间
最后查看时间
```

交互：

```text
新增安全速记
↓
选择分类
↓
录入账号 / 密码 / 备注
↓
保存时加密
```

查看：

```text
默认只显示标题、分类、账号脱敏
点击查看密码
↓
需要二次确认 / 输入本机解锁密码
↓
短时显示
↓
自动隐藏
```

搜索：

* 按标题搜索
* 按分类筛选
* 按标签筛选
* 支持复制账号
* 支持复制密码
* 复制后自动清空剪贴板（可配置）

安全边界：

```text
密码类数据不能明文保存在普通任务、日志、候选任务或附件备注里
前端展示必须默认脱敏
工作日志只记录“查看/复制了某条安全速记”，不记录真实密码内容
线上同步只能同步加密后的密文
后台不能直接看到明文密码
```

加密策略：

```text
本地生成或用户设置一个保险箱主密码
↓
用主密码派生加密密钥
↓
敏感字段在前端加密后再保存
↓
云端只保存密文、盐值、初始化向量和元信息
```

边界：

```text
V2 先实现安全速记的手动录入、搜索、复制、脱敏展示
V3 再考虑自动识别“这像账号密码，是否放入保险箱”
V4 才考虑自动填充网页表单
```

---

## V3（数字秘书版）

目标：

**从提醒工具升级为工作助理。**

### AI工作台

你可以直接问：

```text
今天还有什么没完成？
```

```text
本周最重要的事情是什么？
```

```text
王总的事情进展如何？
```

---

### 自动日报

每天下班：

```text
完成任务

未完成任务

明日重点
```

自动生成。

---

### 自动周报

每周五：

```text
完成任务数

项目进展

遗留事项
```

自动生成。

---

### 项目管理

任务自动归类：

```text
客户A

客户B

采购

财务

行政
```

---

### 长期记忆

例如：

```text
王总总是要求周三前报价

李经理习惯下午开会
```

AI建立工作习惯库。

---

## V4（真正的数字秘书）

目标：

**主动帮你管理工作。**

### 微信主动分析

每天：

```text
扫描全部新增消息
```

识别：

* 待办
* 风险
* 截止日期
* 催办事项

---

### 自动催办

例如：

```text
等待供应商回复
```

超过：

```text
3天
```

提醒：

```text
需要跟进供应商
```

---

### 邮件接入

自动分析：

* Outlook
* Gmail

提取任务。

---

### 日历接入

自动关联：

```text
会议
任务
截止日期
```

---

### AI秘书模式

你直接说：

```text
今天帮我排优先级
```

AI自动生成：

```text
09:00 客户报价
10:30 合同审核
14:00 供应商沟通
16:00 财务确认
```

---

# 最终产品定位

我会把这个项目定义成：

```text
Sherlly Assistant

一句话：

让任何工作事项进入一个不可遗忘的闭环。
```

核心闭环：

```text
发现
↓
记录
↓
提醒
↓
完成
↓
归档
↓
复盘
```

只要 V1 做扎实，你遇到的“微信里说过但忘了做”的问题基本就能解决；后面的 V2~V4 都是在这个闭环上不断增强智能化能力。
