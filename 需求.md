# Sherlly Assistant 产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | Sherlly Assistant |
| 当前应用版本 | `v3.0.0` |
| 文档版本 | `PRD v1.1` |
| 基线日期 | `2026-07-16` |
| 代码基线 | Git `e953e5c` |
| 产品形态 | Windows Electron 桌面端 + Web/移动浏览器适配 |
| 核心目标 | 让工作事项进入不可遗忘的闭环 |
| 本次修订 | 确认 V1 统一 Cloudflare 架构，并细化同步、迁移、备份、附件和诊断要求 |

### 1.1 文档目的

本文档用于统一产品、开发和测试对 Sherlly Assistant 的理解，并回答以下问题：

1. 当前产品已经具备什么能力。
2. 每项能力的业务规则和边界是什么。
3. 后续版本需要补齐什么。
4. 如何判断功能已经满足需求。

### 1.2 状态标记

| 标记 | 含义 |
| --- | --- |
| ✅ 已实现 | 当前代码中已有完整入口和主要业务逻辑 |
| 🟡 部分实现 | 已有基础能力，但仍缺少自动化、跨端或完整异常处理 |
| ⏳ 待实现 | 当前代码中尚无对应能力 |
| 🚫 非当前范围 | 已明确暂不开发，防止需求无边界扩张 |

---

## 2. 产品概述

### 2.1 产品定位

Sherlly Assistant 是面向个人办公场景的数字秘书，重点解决微信、口头沟通和零散工作信息容易被遗忘的问题。

一句话定位：

> 让任何工作事项进入一个不可遗忘、可执行、可复盘的闭环。

核心闭环：

```text
发现
↓
记录
↓
提醒
↓
执行
↓
完成或取消
↓
归档
↓
复盘
```

### 2.2 核心问题

目标用户通常面对以下问题：

1. 微信消息量大，工作安排夹在聊天中，稍后容易找不到。
2. 任务虽然被记录，但缺少持续提醒和明确执行入口。
3. 开始处理任务时，需要重新寻找网址、文件、项目目录或软件。
4. 每日、每周工作结果依赖人工回忆，复盘成本高。
5. 账号、密码、Token 等敏感资料散落在聊天和普通备注中，存在泄露风险。
6. 常用工具和脚本缺少统一入口，查找成本高。

### 2.3 产品目标

#### P0：不可遗忘

- 所有工作事项可以快速进入任务系统。
- 疑似遗漏事项能够进入候选池并触发提醒。
- 未完成任务按照优先级和时间持续提醒。

#### P1：快速执行

- 任务可以绑定网址、路径、VSCode 项目或软件命令。
- 用户点击一次即可打开对应工作环境。
- 执行动作和状态变化自动记录日志。

#### P2：可复盘

- 自动生成今日、本周和本月工作视图。
- 从历史任务提取联系人、项目、排期和积压线索。
- 用规则化 AI 工作台回答常见工作查询。

#### P3：安全沉淀

- 敏感速记在本地加密后保存。
- 云端只接收密文，不接收保险箱主密码或明文内容。
- 查看、复制和打开敏感信息时留下不包含秘密内容的审计日志。

### 2.4 非目标

以下能力不属于当前版本目标：

- 🚫 自动控制微信客户端并读取全部聊天记录。
- 🚫 未经确认自动向联系人发送消息。
- 🚫 自动点击、填写或提交第三方系统表单。
- 🚫 替代专业项目管理、企业审批或财务系统。
- 🚫 服务端代用户解密安全速记。
- 🚫 在没有用户确认的情况下自动删除任务或敏感资料。

---

## 3. 目标用户与使用场景

### 3.1 目标用户

主要用户是需要独立处理大量微信工作事项的个人，包括但不限于：

- 客户对接人员
- 行政和采购人员
- 项目负责人
- 自由职业者
- 需要同时管理多个客户或项目的开发人员

### 3.2 核心用户故事

#### US-01 快速记录

作为用户，我希望收到工作安排后能在几秒内创建任务，以免打断当前工作或遗忘事项。

#### US-02 从微信提取任务

作为用户，我希望粘贴一段微信聊天后，系统能找出疑似待办并让我确认，而不是逐条手动录入。

#### US-03 持续提醒

作为用户，我希望未完成事项根据优先级持续提醒，从而降低“记录了但没做”的概率。

#### US-04 一键开始工作

作为用户，我希望点击“开始执行”就打开任务对应的网址、文件、项目或软件，减少寻找工作环境的时间。

#### US-05 查看工作结果

作为用户，我希望快速看到今天、本周或本月完成了什么、还有什么逾期，以便汇报和安排下一步。

#### US-06 安全保存敏感信息

作为用户，我希望账号、密码和 Token 默认加密、脱敏且短时解锁，避免被他人直接看到。

#### US-07 移动端临时记录

作为用户，我希望在手机浏览器中查看今日重点、快速输入或语音创建任务，并导出到系统日历。

---

## 4. 当前产品范围

### 4.1 已实现能力概览

| 模块 | 状态 | 当前能力 |
| --- | --- | --- |
| 任务看板 | ✅ | 创建、编辑、复制、删除、筛选、搜索和状态推进 |
| 快捷录入 | ✅ | 全局快捷键、拖拽文本、手机速记和图片粘贴 |
| 微信候选池 | 🟡 | 支持粘贴微信文本并按规则识别候选；不自动读取微信 |
| 智能字段识别 | ✅ | 从标题和备注识别截止时间、提醒时间窗、联系人和优先级 |
| 任务执行动作 | ✅ | 打开 URL、文件/目录、VSCode 项目和软件命令 |
| 提醒系统 | ✅ | 页面提醒、系统通知、任务栏闪烁、托盘闪烁和可关闭声音 |
| 每日时段任务 | ✅ | 早上、中午、下午、晚上时段安排、完成记录和遗漏提醒 |
| 附件 | ✅ | 选择本机文件、粘贴图片、图片预览和打开附件 |
| 工作日志与日报 | ✅ | 今日、本周、本月数据汇总和日志明细 |
| AI 任务整理 | ✅ | 重复任务、相似任务和长期未完成任务建议 |
| AI 工作台 | ✅ | 基于本地任务和日志回答预设或自定义问题 |
| 长期记忆 | ✅ | 从历史任务汇总联系人、项目、排期与积压线索 |
| 安全速记 | ✅ | 前端加密、脱敏、短时解锁、复制和审计日志 |
| 工具库 | ✅ | 本地新增、删除、分类、搜索常用工具 |
| 账号系统 | ✅ | 注册、登录、退出、会话恢复和修改密码 |
| 云同步 | ✅ | 同步任务、候选、日志、设置和保险箱密文 |
| Android 原生应用 | ⏳ | 当前只有 Web 移动端适配，无原生 Android 应用 |
| 微信自动扫描 | ⏳ | 当前无自动读取微信或后台扫描能力 |
| 邮件和日历接入 | 🟡 | 支持单任务导出日历；未接入 Outlook、Gmail 或日历双向同步 |
| 自动催办联系人 | ⏳ | 当前只提醒用户，不自动发送催办消息 |

### 4.2 当前 AI 能力边界

当前“AI”能力由本地规则、关键词识别、时间解析、相似度计算和任务统计构成，不依赖外部大模型服务。

这意味着：

- 数据默认不需要发送给第三方 AI 服务。
- 结果可复现、响应快、成本低。
- 对复杂自然语言、隐含语义和开放问题的理解能力有限。
- 后续接入 LLM 时必须增加用户授权、脱敏、失败降级和费用边界。

---

## 5. 核心数据模型

### 5.1 任务 Task

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | 系统生成的唯一标识 |
| `title` | 是 | 任务标题，去除首尾空格后不能为空 |
| `source` | 是 | 手动录入、快捷键速记、手机速记、微信粘贴或拖拽文本 |
| `owner` | 否 | 自己、同事、客户或从文本识别的联系人 |
| `createdAt` | 是 | 创建时间 |
| `updatedAt` | 是 | 最后更新时间 |
| `dueAt` | 否 | 截止时间 |
| `reminderStartAt` | 否 | 预定提醒窗口开始时间 |
| `reminderEndAt` | 否 | 预定提醒窗口结束时间 |
| `priority` | 是 | `high`、`normal`、`low` |
| `status` | 是 | `todo`、`doing`、`waiting`、`done`、`cancelled` |
| `tags` | 否 | 标签数组，保存时去重和清理空值 |
| `note` | 否 | 任务说明 |
| `dailySlots` | 否 | 早上、中午、下午、晚上中的一个或多个时段 |
| `completionRecords` | 否 | 每次完成时段任务的记录 |
| `dailySlotReminderRecords` | 否 | 每日时段已提醒记录，避免同一时段重复提醒 |
| `launchAction` | 否 | 执行动作类型和目标 |
| `attachments` | 否 | 附件名称、路径、类型和添加时间 |
| `lastRemindedAt` | 否 | 最近一次普通任务提醒时间 |
| `completedAt` | 否 | 完成时间 |

### 5.2 候选任务 Candidate

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `text` | 从粘贴文本中识别出的候选内容 |
| `source` | 当前默认为“微信粘贴” |
| `detectedAt` | 识别时间 |
| `remindedAt` | 遗漏提醒时间，确保只提醒一次 |

### 5.3 工作日志 Log

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `action` | 创建、修改、开始执行、完成、删除、查看或复制等动作 |
| `taskId` | 关联对象 ID；安全速记也使用该字段关联 |
| `taskTitle` | 关联对象标题 |
| `detail` | 不包含密码等秘密内容的操作说明 |
| `createdAt` | 日志时间 |

### 5.4 安全速记 VaultItem

明文业务字段：

```text
标题
分类
账号
密码 / Token
网址
备注
标签
```

持久化时，标题、分类和必要元信息用于列表展示；账号、密码、网址、备注等敏感内容以密文保存。加密数据包含：

```text
version
algorithm
kdf
iterations
salt
iv
ciphertext
```

### 5.5 设置 Settings

当前设置包括：

- `soundEnabled`：是否启用提醒声音。
- `ftpClientPath`：FTP/SFTP 类型安全速记使用的客户端路径。

### 5.6 工具 Tool

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `name` | 工具名称，不能为空 |
| `path` | 工具路径或入口，不能为空 |
| `description` | 用途说明 |
| `category` | 脚本、CLI、应用程序、代码库、文档或其他 |
| `createdAt` | 创建时间 |
| `updatedAt` | 更新时间 |

工具库当前独立保存在浏览器 `localStorage`，存储键为 `sherlly_tools_library`，不包含在现有账号云同步数据中。V1 必须将工具纳入统一数据模型，并在首次升级时迁移该独立存储。

### 5.7 V1 同步数据封装 SyncEnvelope

V1 云端读写不再直接传输裸业务对象，而是使用带版本控制的同步封装：

| 字段 | 说明 |
| --- | --- |
| `schemaVersion` | 数据结构版本，用于迁移和兼容判断 |
| `revision` | 服务端单调递增版本；每次成功写入后递增 |
| `updatedAt` | 服务端确认的最近更新时间 |
| `deviceId` | 发起最近一次成功写入的设备标识，不得包含硬件序列号等敏感信息 |
| `checksum` | 规范化业务数据的完整性校验值，并携带算法版本 |
| `data` | 统一业务数据，包含任务、候选、日志、安全速记密文、设置、工具和删除墓碑 |

同步实体必须具有稳定 `id` 和 `updatedAt`。删除操作必须生成 tombstone，至少记录实体类型、实体 ID、删除时间和删除设备；tombstone 在完成所有活跃设备同步或达到明确保留策略前不得清理，防止旧客户端把已删除数据重新上传。

---

## 6. 关键业务流程

### 6.1 手动创建任务

```text
打开任务看板
↓
填写标题和可选属性
↓
系统补充可识别的时间、联系人、标签和优先级
↓
校验标题
↓
创建任务
↓
写入“创建任务”日志
↓
保存到本地或云端
```

异常规则：

- 标题为空时禁止保存并显示错误。
- 非法提醒窗口不保存，结束时间必须晚于开始时间。
- 未配置执行动作时仍允许创建任务。
- 保存失败时应保留当前页面数据，并提示同步或存储错误。

### 6.2 微信文本识别

```text
用户复制微信内容
↓
粘贴到候选识别区域
↓
系统按行拆分文本
↓
使用任务关键词筛选，最多生成 20 条候选
↓
用户确认或忽略
↓
确认：转为正式任务并写日志
忽略：从候选池移除并写日志
```

当前关键词覆盖安排、提醒、记得、处理、确认、跟进、发送、提交、截止、今天、明天、周、月底、报价、合同、开会等工作表达。

边界：当前系统不主动访问微信进程、聊天数据库、屏幕内容或剪贴板历史。

### 6.3 开始执行任务

```text
用户点击“开始执行”
↓
任务状态切换为“进行中”
↓
读取执行动作
↓
调用桌面端能力打开目标
↓
记录开始执行结果
```

执行动作：

- `url`：使用默认浏览器打开网址。
- `path`：使用系统打开文件或文件夹。
- `vscode`：优先调用 `code <项目路径>`，失败时尝试系统打开路径。
- `command`：按命令和参数启动软件。
- `none`：只切换状态并记录“未配置执行动作”。

V1 边界：只负责打开工作环境，不自动点击、填写或提交。

### 6.4 提醒流程

```text
应用运行时每分钟检查任务和候选池
↓
满足提醒条件
↓
页面内提醒 + Windows 通知
↓
任务栏闪烁 + 托盘图标闪烁
↓
根据设置决定是否发声
↓
更新最近提醒时间
```

提醒间隔：

| 优先级 | 间隔 |
| --- | --- |
| 高优先级 | 15 分钟 |
| 普通 | 30 分钟 |
| 低优先级 | 60 分钟 |

规则：

1. 已完成和已取消任务不提醒。
2. 有提醒窗口时，只在窗口内提醒。
3. 有截止时间但无提醒窗口时，从“截止时间减去优先级间隔”开始提醒。
4. 无截止时间和提醒窗口时，从创建或更新时间加一个提醒间隔后开始提醒。
5. 普通任务可按优先级间隔重复提醒，直到任务结束或离开提醒窗口。
6. 候选任务识别 30 分钟后仍未处理时提醒一次。
7. 每日时段任务在对应时段提醒一次，并记录当日已提醒时段。
8. 系统关机、应用退出或浏览器页面关闭期间不保证提醒。

### 6.5 安全速记流程

```text
用户输入保险箱主密码
↓
填写安全速记
↓
使用 PBKDF2-SHA-256 派生密钥
↓
使用 AES-GCM 256 加密敏感字段
↓
只保存密文、盐值、IV、迭代次数和元信息
```

查看流程：

```text
默认脱敏展示
↓
输入本次使用的主密码
↓
前端解密
↓
显示 2 分钟
↓
自动锁定并从页面状态移除明文
```

复制流程：

```text
解锁安全速记
↓
复制账号或密码
↓
写入不含明文的审计日志
↓
30 秒后尝试清空剪贴板
```

边界：主密码不保存；忘记主密码后，现有密文无法恢复。

---

## 7. 功能需求

### 7.1 任务录入

#### FR-TASK-01 手动录入 ✅

用户可以输入标题、来源、负责人、截止时间、提醒窗口、优先级、状态、标签、备注、每日时段、执行动作和附件。

#### FR-TASK-02 全局快捷键速记 ✅

- Windows 桌面端注册 `Ctrl+Alt+S`。
- 触发后打开或聚焦应用，并切换到任务看板。
- 新任务来源标记为“快捷键速记”。

#### FR-TASK-03 拖拽文本创建 ✅

- 用户可将文本拖入页面。
- 文本非空时创建任务草稿或任务。
- 来源标记为“拖拽文本”。

#### FR-TASK-04 移动端速记 ✅

- 移动布局展示今日重点任务。
- 支持文本快速创建任务。
- 浏览器支持 Speech Recognition 时，可使用语音转文字创建任务。
- 支持将任务导出为日历文件。

#### FR-TASK-05 微信自动发现 🟡

当前支持粘贴文本识别，不支持自动扫描微信新增消息。

后续自动扫描必须满足：

- 用户主动开启并可随时关闭。
- 明确显示扫描范围和最近扫描时间。
- 原始聊天内容默认不上传云端。
- 候选任务必须经用户确认后才能进入正式任务。

### 7.2 智能字段识别 ✅

系统从标题和备注中识别：

- 截止时间，例如今天、明天、周几、月底等。
- 提醒时间窗口。
- 联系人或负责人表达。
- 与截止时间相关的优先级。
- 联系人存在时补充“客户”标签。

手动填写的字段优先级高于自动推断结果，系统不得覆盖用户明确选择的高或低优先级。

### 7.3 任务管理 ✅

任务状态：

```text
待办
进行中
等待他人
已完成
已取消
```

支持：

- 创建、编辑、复制和删除任务。
- 按状态范围筛选。
- 按标题、标签等可见信息搜索。
- 打开任务详情。
- 直接切换状态。
- 对每日时段任务记录单次完成，而不是立即结束整个长期任务。
- 显示逾期、等待和每日完成进度。

### 7.4 每日时段任务 ✅

可选时段：

| 时段 | 时间范围 |
| --- | --- |
| 早上 | 06:00–12:00 |
| 中午 | 12:00–14:00 |
| 下午 | 14:00–18:00 |
| 晚上 | 18:00–24:00 |

规则：

- 一个任务可以选择多个时段。
- 每个时段每天最多记录一次有效完成。
- 时段结束后仍未完成则计为遗漏。
- 日报展示完成、遗漏和剩余次数。

### 7.5 附件 ✅

支持：

- 桌面端选择一个或多个本机文件。
- 从剪贴板粘贴图片。
- 预览不超过 8MB 的本机图片。
- 在系统默认程序中打开附件。
- 从任务中移除附件引用。

存储边界：

- 本机文件附件只保存路径，不上传文件二进制。
- 文件移动或删除后，原路径可能失效。
- 粘贴图片以 Data URL 嵌入任务数据，可能增加同步数据体积。
- V1 必须对单个 Data URL、单条任务附件总量和账号同步数据总量设置可配置上限；前端应在保存前提示占用并阻止超限数据进入同步队列。
- 客户端与服务端必须使用同一组限制；服务端拒绝超限请求时返回明确错误码，且不得修改当前 revision。
- V1 不上传本机路径对应的文件内容；诊断页只报告路径是否可访问，不上传路径或文件内容。
- 云附件预留 R2 对象元数据，包括对象键、文件名、MIME、大小、checksum、创建时间和访问状态，但 R2 二进制上传属于 V2。

### 7.6 工作日志与报告 ✅

自动记录：

- 创建、修改、开始执行、完成、取消和删除任务。
- 确认或忽略候选任务。
- 创建、修改、查看、复制、打开和删除安全速记。
- AI 整理产生的状态或备注变化。

报告范围：

```text
今日日志
本周日志
本月日志
```

报告内容包括：

- 创建、开始和完成数量。
- 当前活动任务、等待任务和逾期任务。
- 每日时段任务遗漏次数。
- 已完成事项、未完成事项和下一步重点。
- 最近日志明细。

### 7.7 AI 任务整理 ✅

系统根据任务标题、备注、状态和更新时间生成以下建议：

- 重复任务：允许保留主任务并将其他任务标记为已取消。
- 相似任务：允许合并内容到主任务备注，并取消被合并任务。
- 长期未完成任务：当前阈值为 14 天，允许标记说明并调整为低优先级。

所有整理动作必须由用户主动触发，系统不能静默修改任务。

### 7.8 AI 工作台 ✅

预设问题包括：

```text
今天还有什么没完成？
本周最重要的事情是什么？
等待他人的事情有哪些？
```

系统根据本地任务和日志生成回答，并展示结果依据。查询无法识别时，应返回可理解的提示，而不是编造不存在的任务。

### 7.9 长期记忆 ✅

长期记忆从历史任务中提取：

- 联系人相关任务数量和常见安排。
- 项目或标签积压情况。
- 常见截止日期与工作时段。
- 长期未动任务。

当前长期记忆是可重算的统计结果，不是独立保存的隐式用户画像。

### 7.10 安全速记 ✅

分类：

```text
网站账号
客户系统
软件授权
网络/VPN
FTP/SFTP
API/Token
其他
```

功能：

- 新增、编辑、删除安全速记。
- 按标题搜索、按分类筛选、按标签筛选。
- 默认脱敏展示账号和秘密内容。
- 输入主密码后短时查看。
- 复制账号或密码。
- URL 使用默认浏览器打开。
- FTP/SFTP 地址可通过配置的客户端打开。

安全要求：

- 主密码至少 6 位；后续版本应提升密码强度要求并支持设备解锁。
- 主密码不得写入日志、本地存储、云端或错误上报。
- 工作日志不得包含账号密码、Token、备注明文或解密结果。
- 服务端不得提供解密接口。
- 复制后 30 秒尝试清空剪贴板，但受操作系统和浏览器权限限制，不保证绝对成功。

### 7.11 工具库 ✅

支持：

- 新增和删除常用工具。
- 按名称和说明搜索。
- 按分类过滤。
- 保存工具路径、描述和分类。

当前边界：

- 工具只保存在当前浏览器或桌面渲染进程的 `localStorage`。
- 工具库暂不随账号同步。
- 当前工具库与任务执行动作没有自动关联。

V1 要求：

- 工具必须加入统一业务数据的 `tools` 集合，并随账号同步、导出和备份。
- 首次升级时读取 `sherlly_tools_library`，按工具 ID 合并到统一数据；迁移成功前不得删除旧键。
- 同 ID 工具冲突遵循统一实体冲突规则；名称或路径相同但 ID 不同的工具只提示疑似重复，不得静默删除。
- 迁移成功并完成一次可验证持久化后，记录迁移标记；重复启动迁移必须保持幂等。

### 7.12 账号与云同步 ✅

账号能力：

- 注册。
- 登录。
- 查询当前会话。
- 修改密码。
- 退出登录。
- 注册和登录入口支持 Turnstile；生产环境启用时必须由 Worker 调用 Siteverify 做服务端验证，不能只信任前端结果。

同步范围：

```text
任务
候选任务
工作日志
安全速记密文
用户设置
工具库
删除墓碑
```

当前实现：

- 配置云 API 时，登录是访问用户数据的前提。
- 首次登录可合并有意义的本地数据与云端数据。
- 云端保存失败时，数据回退到当前账号隔离的本地存储。
- 身份失效时清除本地认证信息并要求重新登录。
- 未配置云 API 时，Electron 使用本地数据文件；普通 Web 环境使用 `localStorage`。
- Cloudflare `/api/data` 当前以整包 `GET/PUT` 读写 `SHERLLY_DATA` KV，没有 revision、条件写入或冲突响应。
- Node/Express + MongoDB 仅作为既有兼容实现和迁移来源，不再作为 V1 推荐生产架构。

V1 统一 Cloudflare 生产架构：

- Cloudflare Worker 作为唯一公开 API 网关，负责认证、输入校验、CORS、限流、错误映射和绑定调用。
- `SHERLLY_AUTH_STORE` Durable Object 继续负责账号、密码摘要和会话；认证数据不得回退到浏览器可读存储。
- 新增 `SHERLLY_USER_DATA` Durable Object binding，使用 `getByName(userId)` 或等价的稳定路由，让每个用户进入独立实例并作为业务数据 authoritative store。
- `SHERLLY_USER_DATA` 使用 SQLite-backed Durable Object 的强一致存储和事务完成 revision 检查、写入、迁移标记与备份索引更新；禁止以单个全局实例承载所有用户业务数据。
- `SHERLLY_DATA` KV 仅用于读取旧版 `appData:{userId}`、保留迁移回滚快照和兼容期审计，不再作为业务数据唯一权威写入源。
- R2 预留为 V2 云附件对象存储；V1 不得把附件二进制写入 KV 或 Durable Object。
- `wrangler.jsonc` 必须声明新的 Durable Object binding 和 migration；生产 `CORS_ORIGIN` 必须收敛为受信任来源，不能继续使用通配符。

#### FR-V1-SYNC-01 条件写入与 revision

- `GET /api/data` 返回完整 `SyncEnvelope`。
- `PUT /api/data` 必须携带 `baseRevision`、`deviceId`、`schemaVersion` 和业务数据。
- 服务端仅在 `baseRevision` 等于当前 revision 时接受写入，并在同一事务中把 revision 增加 1。
- revision 不匹配时返回 `409 Conflict`，响应至少包含当前 revision、当前更新时间和可重新拉取的标识；服务端不得覆盖任何数据。
- 客户端解决冲突后必须基于最新 revision 重试，禁止无条件覆盖接口。

#### FR-V1-SYNC-02 实体合并与删除

- 不同实体 ID 的并发新增或修改可以按实体自动合并。
- 同一实体在本地和云端都发生变化时，不得仅按客户端时钟静默覆盖；应提供“保留本地”“保留云端”“复制为新项”或适用的字段级合并入口。
- 删除使用 tombstone 参与合并；旧设备上传的存量实体不得覆盖时间更新的 tombstone。
- 设置对象按字段保存更新时间或等价的变更元数据，避免一个设备修改声音设置时覆盖另一设备修改的其他设置。

#### FR-V1-SYNC-03 同步状态与离线队列

界面必须展示以下状态：

```text
同步中
已同步
离线待同步
存在冲突
同步失败
```

- 展示最近一次成功同步时间、当前本地 revision 和待同步变更数量。
- 离线修改先写入账号隔离的本地持久化和变更队列，联网后按顺序重试。
- 重试必须幂等；应用崩溃或重复提交不得生成重复任务、日志、工具或 tombstone。
- 用户可以手动重试、查看已脱敏错误原因，并在冲突未解决时继续离线使用。

#### FR-V1-SYNC-04 KV 到 Durable Object 迁移

- 用户业务 Durable Object 首次为空时，由 Worker 读取 KV 的 `appData:{userId}` 旧数据并规范化。
- 导入前创建带时间、来源 schema 和 checksum 的回滚快照；导入成功后在 Durable Object 内原子记录迁移完成标记。
- 迁移过程可重复执行但结果必须幂等；中断后不得同时存在两个权威版本。
- 兼容期内旧客户端写入 KV 时，系统必须拒绝、只读或明确标记为待人工导入，禁止静默覆盖 Durable Object 新版本。
- 完成迁移验证前不得删除 KV 原始数据；清理时间和保留周期必须写入运维文档。

#### FR-V1-SYNC-05 本地与 MongoDB 数据迁移

- Electron 本地数据文件、账号隔离 `localStorage`、旧全局回退键和工具库独立键均可作为一次性迁移来源。
- MongoDB 兼容服务必须提供受认证的标准 JSON 导出，或由受控迁移脚本转换为同一 `schemaVersion`；V1 不实施 Cloudflare 与 MongoDB 双写。
- 导入前展示来源、数据数量、冲突数量和预计结果；用户确认后才执行合并或替换。
- 迁移成功以云端 revision 提升、重新读取 checksum 一致和本地迁移标记写入三项同时成立为准。

### 7.13 数据导出、导入与备份 🟡

#### FR-V1-BACKUP-01 手动导出

- 用户可导出包含 `SyncEnvelope`、导出时间、应用版本和数据统计的 JSON 文件。
- 安全速记只导出密文、KDF 参数、salt、iv 和必要元数据，不得导出主密码或解密明文。
- 导出前明确提示本机附件只包含路径引用，Data URL 图片会包含在文件中，未来 R2 对象只导出元数据清单。

#### FR-V1-BACKUP-02 导入

- 导入必须先校验 JSON 格式、`schemaVersion`、checksum、实体 ID 和必要字段，再展示预览。
- 用户可选择“合并”或“替换”；替换属于破坏性操作，必须二次确认并先创建自动备份。
- 合并遵循同步冲突与 tombstone 规则，禁止以数组拼接方式制造重复实体。
- 导入失败必须保持导入前 revision 和数据不变，并提供不含敏感内容的错误报告。

#### FR-V1-BACKUP-03 自动备份

- schema 迁移、批量删除、替换导入和检测到数据数量显著下降的保存前，必须创建自动备份。
- 备份策略必须配置保留数量或保留期限，并允许用户查看时间、来源、数据统计和 checksum。
- 恢复备份不直接覆盖当前数据，必须走导入预览、二次确认和 revision 条件写入流程。

### 7.14 运行诊断与失败降级 🟡

诊断页至少检查：

- 云 API 可达性、认证状态、服务端 schema、当前 revision、最近同步时间和待同步数量。
- 通知权限为允许、拒绝或未决定，并提供对应系统设置指引。
- 全局快捷键是否注册成功；被其他程序占用时显示冲突而不是假报成功。
- 本机附件路径是否存在、Data URL 是否有效以及是否接近配置上限。
- Electron preload 必要能力是否存在，自动更新源是否可访问。

诊断信息不得包含登录 Token、主密码、安全速记明文、完整本机路径、MongoDB 连接串或服务端密钥。单项失败不得导致诊断页崩溃；云端失败时，本地任务、日志、工具和安全速记密文仍可读写，并明确显示“离线待同步”。

### 7.15 桌面端能力 ✅

- 系统托盘入口。
- 全局快捷键。
- Windows 通知、任务栏闪烁和托盘闪烁。
- 本机文件选择、预览和打开。
- 启动 URL、路径、VSCode 和软件命令。
- 检查、下载和安装 GitHub Release 更新。

---

## 8. 安全与隐私要求

### 8.1 数据分级

| 级别 | 数据 | 要求 |
| --- | --- | --- |
| 普通 | 任务标题、状态、标签、日志 | 可按用户账号同步 |
| 受限 | 联系人、备注、附件路径 | 仅用于产品功能，避免不必要展示 |
| 敏感 | 账号、密码、Token、VPN 和服务器信息 | 前端加密后保存，默认脱敏 |
| 秘密 | 保险箱主密码、服务端密钥、数据库连接串 | 不持久化或仅存在安全服务端环境中 |

### 8.2 强制安全规则

1. 桌面端和 Web 前端不得直接连接 MongoDB。
2. 安全速记敏感字段不得明文进入任务、候选池、普通备注或日志。
3. 云端只同步加密后的安全速记。
4. 登录 Token 不得出现在页面正文、日志或公开错误信息中。
5. 打开本机路径或执行命令前必须由用户明确配置并主动触发。
6. 自动扫描微信、邮件或日历前必须获得单独授权。
7. 后续接入 LLM 时，默认只发送完成问题所需的最少数据。
8. Turnstile Token 必须在 Worker 服务端通过 Siteverify 验证，并校验预期 hostname 或等价来源信息；验证失败不得创建会话。
9. 生产环境 CORS 只允许明确配置的受信任来源；管理凭据、Turnstile secret、Durable Object 数据和 R2 凭据不得暴露给前端。
10. `deviceId` 只能是应用生成的随机标识，不得使用 MAC、磁盘序列号、Windows SID 等稳定硬件或系统身份。
11. checksum 用于完整性和迁移验证，不能替代认证、授权、加密或服务端 revision 检查。
12. 导出、备份、诊断和错误上报必须保持保险箱零知识边界，不得在服务端增加解密能力。

### 8.3 已知风险

- 主密码只有最小长度校验，尚无强度评分、恢复机制或系统生物识别。
- 剪贴板清空是尽力而为，其他应用可能已读取复制内容。
- 本机附件路径可能包含用户名、目录结构等隐私信息。
- Data URL 图片会进入任务同步数据，可能造成存储膨胀。
- 工具路径和命令执行能力需要防止误配置，不应从不可信文本自动生成。
- 现有 KV 业务数据具有 eventual consistency，迁移完成前仍可能发生旧客户端读到旧值或延迟传播。
- 客户端设备时间可能不准确，实体 `updatedAt` 不能单独承担并发控制，必须以服务端 revision 为准。
- 多设备同时修改同一实体可能需要人工解决；系统应优先防止静默丢失，而不是追求无提示自动合并。
- 本地回退、导出文件和自动备份扩大了数据副本数量，必须提供清理策略并明确存放位置。

---

## 9. 非功能需求

### 9.1 可用性

- 核心创建任务流程在正常情况下不超过 3 个主要操作步骤。
- 关键操作应提供成功或失败反馈。
- 弹窗和附件预览支持关闭，切换主视图时不得残留遮挡层。
- 桌面端离线时仍可使用本地数据和主要任务功能。

### 9.2 性能

- 任务列表、搜索和筛选在个人规模数据下应即时响应。
- 提醒检查频率维持为每分钟一次，避免高频轮询。
- 本机图片预览单文件限制为 8MB。
- 日志和每日提醒记录应设置保留或裁剪策略，避免无限增长。
- 同步请求必须在序列化前检查配置的数据量上限，避免浏览器或 Worker 因超大 Data URL 数据耗尽内存。
- 用户业务 Durable Object 按用户 ID 分片；禁止所有账号共享一个业务数据实例形成串行瓶颈。
- 诊断页的附件检查采用限流或分批执行，不能因大量失效路径阻塞主界面。

### 9.3 可靠性

- 保存前必须完成数据规范化。
- 云同步失败不得静默丢弃本地修改。
- 自动更新失败不得阻止用户继续使用当前版本。
- 应用重启后任务、候选、日志、设置、安全速记密文、工具、tombstone 和离线队列可恢复。
- 所有云端写入必须使用 `baseRevision` 条件检查；`409`、超限、鉴权失败或校验失败均不得改变服务端当前数据。
- 迁移、重试和备份恢复必须幂等，可在请求超时、应用崩溃或 Worker 重试后安全继续。
- schema 升级失败必须回滚到升级前快照，不能把部分迁移结果标记为成功。
- 本地队列成功同步后，只有在重新读取或服务端响应确认 revision 与 checksum 后才能清理对应变更。

### 9.4 可维护性

- 领域规则集中在 `src/lib/domain.js`。
- 存储与同步集中在 `src/lib/storage.js`。
- 保险箱加密集中在 `src/lib/vaultCrypto.js`。
- 桌面系统能力通过 Electron preload 暴露最小 IPC 接口。
- 新功能必须增加对应验收条件，避免只写界面描述。
- 同步 envelope、schema migration、冲突合并和 tombstone 规则必须有独立模块及自动化测试，不能散落在 UI 组件中。
- `wrangler.jsonc` 的 binding、Durable Object migration 与 Worker 导出类必须保持一致，并纳入部署前静态校验。
- Cloudflare 与 Node/MongoDB 兼容实现共用同一导出 schema；兼容代码不得反向限制 Cloudflare authoritative store 的 revision 语义。

---

## 10. 验收标准

### AC-01 创建和编辑任务

- Given 用户输入非空标题，When 保存任务，Then 任务出现在看板并生成创建日志。
- Given 用户未输入标题，When 保存任务，Then 系统阻止保存并提示标题不能为空。
- Given 用户编辑已有任务，When 保存，Then 保留任务 ID 和创建时间，并更新修改时间与日志。

### AC-02 状态闭环

- Given 活动任务，When 切换为已完成，Then 记录完成时间并停止普通提醒。
- Given 活动任务，When 切换为已取消，Then 任务保留在历史记录中且停止提醒。
- Given 已完成或已取消任务，When 到达原提醒时间，Then 不产生提醒。

### AC-03 候选任务池

- Given 用户粘贴包含任务关键词的多行文本，When 执行识别，Then 每个匹配行生成候选且单次最多 20 条。
- Given 候选任务未处理满 30 分钟，When 应用执行提醒检查，Then 只产生一次遗漏提醒。
- Given 用户确认候选，When 转为任务，Then 候选被移除、正式任务被创建并写入日志。

### AC-04 提醒

- Given 高优先级活动任务，When 到达提醒时间，Then 页面和系统产生提醒，并至少间隔 15 分钟再次提醒。
- Given 用户关闭声音，When 产生提醒，Then 视觉提醒保留且通知静音。
- Given 任务设置有效提醒窗口，When 当前时间不在窗口内，Then 不提醒。

### AC-05 执行动作

- Given 任务配置 URL，When 点击开始执行，Then 使用默认浏览器打开并将任务设为进行中。
- Given 任务配置本机路径，When 在 Web 环境点击，Then 明确提示需要 Electron 桌面端。
- Given 未配置动作，When 点击开始执行，Then 仍将任务设为进行中并记录未配置动作。

### AC-06 附件

- Given 用户选择本机文件，When 保存任务，Then 附件名称和路径随任务保存。
- Given 图片小于等于 8MB 且文件存在，When 点击预览，Then 显示图片预览。
- Given 文件不存在或已移动，When 点击预览或打开，Then 显示可理解的失败信息。

### AC-07 安全速记

- Given 主密码少于 6 位，When 保存，Then 系统拒绝加密并提示最小长度。
- Given 安全速记已保存，When 检查持久化数据，Then 不存在账号、密码、网址和备注明文。
- Given 用户输入正确主密码，When 解锁，Then 显示明文且 2 分钟后自动隐藏。
- Given 用户复制密码，When 查看日志，Then 日志只记录复制动作，不包含密码内容。
- Given 复制成功，When 经过 30 秒，Then 系统尝试清空剪贴板。

### AC-08 云同步

- Given 已配置云 API 且用户登录，When 数据变化，Then 任务、候选、日志、设置、保险箱密文、工具和 tombstone 保存到用户云数据。
- Given 云端暂时不可用，When 保存数据，Then 当前账号的本地回退数据仍可恢复。
- Given 登录失效，When 请求云数据，Then 清除失效认证并要求重新登录。
- Given 客户端以当前 `baseRevision` 保存，When Durable Object 事务提交成功，Then 返回 revision 增加 1 且重新读取 checksum 一致。
- Given 两台设备基于同一 revision 修改数据，When 第一台已成功提交而第二台继续提交，Then 第二台收到 `409` 且服务端保留第一台结果。
- Given tombstone 比旧设备中的实体更新，When 旧设备重新同步，Then 已删除实体不得重新出现。

### AC-09 日报和 AI

- Given 存在任务和日志，When 切换今日、本周或本月范围，Then 汇总数据只包含对应时间范围。
- Given 系统识别出重复、相似或长期未完成任务，When 用户未确认整理动作，Then 原任务不得被修改。
- Given AI 工作台回答问题，When 展示结果，Then 同时展示匹配的任务或日志依据。

### AC-10 桌面端稳定性

- Given 用户切换主视图，When 之前打开任务详情或附件预览，Then 遮挡层自动关闭。
- Given 新版本检查失败，When 用户继续操作，Then 当前版本核心功能仍可使用。
- Given 快捷键注册成功，When 用户按 `Ctrl+Alt+S`，Then 应用聚焦并进入快速创建状态。

### AC-11 Cloudflare 数据迁移

- Given 用户 Durable Object 尚无业务数据且 KV 存在 `appData:{userId}`，When 用户首次访问 V1，Then 系统创建回滚快照、导入规范化数据并原子记录迁移完成。
- Given 迁移在提交前中断，When 用户再次访问，Then 系统可安全重试且不会生成重复实体或两个权威版本。
- Given Durable Object 已完成迁移，When 旧客户端尝试写 KV 数据，Then 该写入不得静默覆盖当前 Durable Object revision。
- Given MongoDB 或本地文件导出数据，When 用户导入，Then 先展示统计与冲突预览，用户确认后才写入云端。

### AC-12 导出、导入与备份

- Given 用户导出账号数据，When 检查 JSON，Then 文件包含 schema、revision、统计和 checksum，且安全速记仍为密文。
- Given 导入文件 checksum 或必要字段无效，When 用户尝试导入，Then 系统拒绝且原 revision 和数据不变。
- Given 用户选择替换导入，When 二次确认完成，Then 系统先创建自动备份，再以条件写入提交新 revision。
- Given 用户恢复历史备份，When 未完成预览和确认，Then 当前数据不得被覆盖。

### AC-13 工具库统一同步

- Given `sherlly_tools_library` 存在旧工具，When 用户首次升级，Then 工具按 ID 合并到统一数据并随云端 envelope 返回。
- Given 迁移保存未被云端确认，When 检查旧存储，Then `sherlly_tools_library` 仍保留，避免迁移失败导致丢失。
- Given 两台设备新增不同工具，When 发生 revision 冲突并合并，Then 两个工具均保留且不会重复生成。

### AC-14 附件配额与未来 R2 边界

- Given Data URL 超过单文件或账号配置上限，When 用户保存，Then 客户端阻止入队并显示占用；若绕过客户端，服务端拒绝且 revision 不变。
- Given 本机附件路径失效，When 运行诊断，Then 只显示脱敏后的失效状态，不上传路径或文件内容。
- Given V1 数据导出，When 任务含本机附件或预留 R2 附件，Then 导出仅包含路径引用或对象元数据，不伪装为完整二进制备份。

### AC-15 诊断、权限与失败降级

- Given 通知权限被拒绝，When 运行诊断，Then 显示拒绝状态和设置指引，页面提醒仍可工作。
- Given `Ctrl+Alt+S` 被其他程序占用，When Electron 注册失败，Then 诊断显示快捷键冲突且界面不标记为注册成功。
- Given 云 API 不可用，When 用户创建任务或工具，Then 数据进入账号隔离的离线队列，状态显示“离线待同步”。
- Given 诊断完成，When 查看结果或日志，Then 不包含 Token、主密码、安全速记明文、完整本机路径或服务端 secret。

---

## 11. 版本规划

### 11.1 当前基线：v3.0.0

目标：形成“记录—提醒—执行—完成—复盘”的可用闭环。

已覆盖：

- 任务看板与多入口录入。
- 候选任务池和遗漏提醒。
- 执行动作、附件、日志和报告。
- 本地规则型 AI 工作台、任务整理和长期记忆。
- 安全速记、账号、云同步和桌面自动更新。

### 11.2 下一阶段：V1 稳定版

优先级：P0。

目标：先把现有闭环做稳定，而不是继续堆叠入口。

- 增加领域规则和关键流程自动化测试。
- 以 Cloudflare Worker 作为唯一推荐生产 API，以按用户分片的 Durable Object 作为业务数据 authoritative store。
- 将现有 KV、Electron 本地文件、账号隔离 `localStorage`、工具库独立键和 MongoDB 导出迁移到统一 schema；不实施双写。
- 引入 `SyncEnvelope`、revision 条件写入、tombstone、离线队列、冲突解决、最后同步时间和手动重试入口。
- 限制 Data URL 单项与总量，提供存储占用提示，并为 V2 R2 云附件预留元数据模型。
- 增加统一 JSON 导出、合并/替换导入、破坏性操作前自动备份和受控恢复。
- 补充通知权限、快捷键冲突、附件失效、同步状态和 schema 兼容诊断。
- 将工具库纳入统一存储、同步、迁移、导出和备份范围。
- 生产启用 Turnstile 服务端 Siteverify、收敛 CORS，并补充 API 限流与脱敏错误策略。

### 11.3 V2：减少人工确认

优先级：P1。

- 提升自然语言截止时间和联系人识别准确度。
- 提供智能优先级建议及可撤销说明。
- 实现原生 Android 或 PWA 的可靠提醒和离线能力。
- 增加安全速记强密码策略、设备解锁和可选剪贴板清理时间。
- 支持云附件对象存储。
- 支持工具库与任务执行动作关联。

### 11.4 V3：工作助理（已实现）

优先级：P2。

- 可选接入 LLM，增强开放问题理解和日报、周报生成。
- 任务自动归类到客户、项目、采购、财务和行政等工作域。
- 将长期记忆升级为可查看、可修改、可删除的工作习惯库。
- 自动识别相似任务并给出合并建议，但仍需用户确认。
- 支持安全速记候选识别，不自动保存敏感内容。

### 11.5 V4：主动数字秘书

优先级：P3。

- 在明确授权和隐私评审后接入微信新增消息分析。
- 接入 Outlook、Gmail 和日历，提取会议、任务和截止日期。
- 对“等待他人”任务提供跟进提醒和催办草稿。
- 根据优先级、截止日期和用户习惯生成日程建议。
- 自动填写或提交第三方系统仍需单独安全方案和逐次确认。

---

## 12. 待决策事项

### 12.1 已确认决策

1. V1 唯一推荐生产云端方案为 Cloudflare Worker；用户业务数据迁移到按用户分片的 Durable Object。
2. 工具库属于账号统一数据，纳入同步、迁移、导出和备份。
3. V1 继续兼容受限 Data URL；云附件二进制在 V2 使用 R2，不写入 KV 或 Durable Object。
4. Node/MongoDB 保留为兼容与迁移来源，不与 Cloudflare 双写或并列作为推荐生产方案。

### 12.2 后续待决策

1. Data URL 单项、单任务和账号总量的生产默认值，以及不同套餐是否采用不同限额。
2. 自动备份保留数量、保留期限、用户主动删除策略和 KV/R2 最终落点。
3. V2 R2 附件采用直接上传、Worker 代理还是预签名 URL，以及对象生命周期和下载授权方案。
4. 原生 Android、PWA 和响应式 Web 中哪一种作为移动端主路线。
5. 微信自动发现采用用户粘贴增强、OCR、无障碍接口还是官方能力。
6. LLM 是否允许接收任务正文；如允许，采用何种脱敏和用户授权策略。
7. 安全速记是否增加恢复密钥；增加后如何避免降低零知识安全边界。

---

## 13. 需求追踪

| 需求模块 | 主要实现位置 |
| --- | --- |
| 任务、提醒、AI 规则、报告、长期记忆 | `src/lib/domain.js` |
| 页面交互与业务编排 | `src/App.jsx` |
| 本地存储、账号和云同步客户端 | `src/lib/storage.js` |
| 安全速记加解密 | `src/lib/vaultCrypto.js` |
| 工具库本地数据 | `src/lib/toolsLibrary.js` |
| 工具库界面 | `src/components/ToolsLibraryPanel.jsx` |
| Electron 快捷键、通知、托盘、执行动作、附件和更新 | `electron/main.cjs` |
| Electron 安全桥接 | `electron/preload.cjs` |
| Node/MongoDB 兼容 API 与迁移来源 | `server/index.cjs` |
| Cloudflare Worker API、认证与现有 KV 迁移逻辑 | `cloudflare/worker.js` |
| Cloudflare binding、Durable Object migration、环境变量与 CORS | `wrangler.jsonc` |
| V1 用户业务 authoritative store | 目标：`SHERLLY_USER_DATA` SQLite-backed Durable Object，按用户 ID 分片 |
| V1 旧数据与回滚快照 | `SHERLLY_DATA` KV，目标职责仅限迁移、兼容和备份 |
| V2 云附件对象 | 目标：R2 bucket 与受控访问 API |

---

## 14. 最终成功标准

产品成功不以“功能数量”衡量，而以闭环结果衡量：

```text
用户收到一个工作事项
↓
可以在几秒内记录或确认
↓
系统在合适时间持续提醒
↓
用户一键进入工作环境
↓
任务明确完成、取消或继续等待
↓
系统自动形成可复盘记录
```

只要这个闭环稳定、可信且不会泄露用户数据，Sherlly Assistant 就完成了数字秘书最重要的第一步。
